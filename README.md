# miniguys

## How to build and run
```
git submodule update --init --recursive
cmake -B build
cmake --build build
./build/miniguys
``` 

## Important notes
### Dependencies
- sdl3 
- cmake
- clang-format
